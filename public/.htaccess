<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Chuyển hướng tất cả các yêu cầu đến index.php nếu không phải là một file hoặc thư mục tồn tại
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
</IfModule>
